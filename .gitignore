# Python
__pycache__/
*.py[cod]
.venv/
venv/
.venv-build/
.pytest_cache/

# Env & secrets
.env

# Deployed/seeded config (per-machine; example IS committed)
config.yaml

# Regenerable data (never hand-edit, never commit)
*.db
*.db.bak*
data/

# OS / editor noise
Thumbs.db
.DS_Store
tmp/

# Build artefacts (PyInstaller onedir + zip)
build/
dist/
build.log

# SDD workspace (subagent-driven development ledger)
.superpowers/
